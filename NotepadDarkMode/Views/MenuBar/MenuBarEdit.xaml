<ResourceDictionary
    x:Class="NotepadDarkMode.Views.MenuBar.MenuBarEdit"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
>
    <MenuItem x:Key="MenuBarEdit" Header="_Edit">
        <MenuItem
            Header="_Undo"
            InputGestureText="Ctrl+Z"
        />
        <MenuItem
            Header="_Redo"
            InputGestureText="Ctrl+Shift+Z"
        />
        <Separator />
        <MenuItem
            Header="Cu_t"
            InputGestureText="Ctrl+X"
        />
        <MenuItem
            Header="_Copy"
            InputGestureText="Ctrl+C"
        />
        <MenuItem
            Header="_Paste"
            InputGestureText="Ctrl+V"
        />
        <!-- The delete alt key is different to default Notepad. -->
        <MenuItem
            Header="_Delete"
            InputGestureText="Del"
        />
        <Separator />
        <MenuItem
            Header="_Find..."
            InputGestureText="Ctrl+F"
        />
        <!-- Find Next and Find Previous? -->
        <MenuItem
            Header="Find and _Replace..."
            InputGestureText="Ctrl+H"
        />
        <Separator />
        <MenuItem
            Header="Select _All"
            InputGestureText="Ctrl+A"
        />
    </MenuItem>
</ResourceDictionary>
