<Window
    x:Class="NotepadDarkMode.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="clr-namespace:NotepadDarkMode.Views"
    xmlns:vm="clr-namespace:NotepadDarkMode.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:NotepadDarkMode"
    mc:Ignorable="d"
    Title="Notepad Dark Mode"
    Height="450"
    Width="800"
>
    <Window.DataContext>
        <vm:MainVM />
    </Window.DataContext>

    <Grid
        Background="#2d2d2d"
    >
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <views:TextEditor Grid.Row="0" DataContext="{Binding TextStateVM}" />

        <TextBlock Grid.Row="1" Text="{Binding TextStateVM.TextEditorText}" />

    </Grid>
</Window>
